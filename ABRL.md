# ABRL Status

Current gate:

```bash
python3 tools/bandit.py check
```

Initial status:

- Lean package compiles.
- Default harness is hierarchical.
- LML theorem-card memory is initialized.
- No website is included.
- Mathlib/LML import is intentionally deferred until a task opts in.
