# Accepted locally: arbitrary convex barycenter

Same-model sequential semantic reviewer, GPT-6 Astra/medium. Accepted-local for all three frozen online-barycenter-v1 targets, including the exact online-jensen-v2 arbitrary-convex barycenter terminal. This is a dependency milestone, not full Jensen, Chapter2 or the total16-chapter Goal.

The proof uses closure membership only as an intermediate, a nonzero supporting functional, almost-sure equality, an integrable lift of centered X into its proper kernel, zero kernel mean, and strong induction on finite dimension. The original convex set need not be closed, have interior, or support a discrete law. Empty-interior separation uses the proper affine span. The local elaboration transparency option does not change kernel validation or assumptions. Frozen headers/context and source hash are unchanged.

Evidence: leaf05 core compile; public-canary02 8929jobs and standard axioms only for three public targets and two canaries; three native fences/context; root/Tests9015jobs; full harness428tests with7existing skips;16registrytests; read-only frontier shadow; final lean-verified site758pages/9867Leanlinks. The original full-root exporter yields3 scoped nodes,339 boundary nodes,544 edges and9 asserted proof-value dependencies; its source hash matches frozen-check.json. One initially expected dependency lacked its MeasureTheory namespace; the extractor was corrected to the actual graph name, with no proof/source change. Rejected elaboration attempts and canary01 sorryAx diagnostics are retained as failures, not accepted evidence.

The canary is a nonconstant probability two-atom vector on a nonclosed convex ray in a proper line, with exact mean(2,0). It exercises the public terminal and proves nonclosedness. This is not an infinite-expectation Jensen canary.

Full Jensen still requires the global affine minorant, finite negative part and final source endpoint. The newly frozen interior-domain minorant helper compiles only as a scratch candidate; its stronger premise is not substituted into the full parent. Runtime commands enforce their individual gates; roles and semantic reviews are separately recorded conventions. No independent external review, merge or deployment claimed. PR delivery follows on PR125 exact be98a5658d882f89bd500a00fab5e1b0553e55f5. Worktree and shared links/caches retained.
