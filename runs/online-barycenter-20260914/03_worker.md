Integrated exact headers/context reviewed before proof. Select strong-induction barycenter body; support bodies unchanged. Initial kernel lift uses an if-test with zero outside the a.e. kernel event, preserving the source random vector after centering almost surely.

Leaf01: full induction body elaborates except ContinuousENorm and CompleteSpace on the local kernel subtype. Leaf02 installs explicit inherited normed/finite-dimensional/complete instances. No header or mathematical premise change.

Independent probes reproduced the failure outside induction. Trace identifies subtype topology versus the induced-norm topology selected by Submodule.normedAddCommGroup; the normative topology probe resolves ContinuousENorm/IsUniformAddGroup. Leaf03 locally selects the inherited norm uniformity/topology before using K and derives completeness by finite dimension. These are the subspace norm structures, not additional source premises. Rejected probes retained.

Leaf03 leaves only T2Space at finite-dimensional completeness: also select the same inherited-norm MetricSpace so its Hausdorff instance matches the already selected topology (leaf04). No mathematical target changes.

Leaf04 exposed another induced/subtype structure matching failure at ContinuousSMul. A clean independent probe with backward.isDefEq.respectTransparency=false passes all original instances, matching use of this elaboration option in pinned mathlib. Leaf05 removes every manual instance override and scopes only this elaboration option to the theorem body. Kernel checking remains unchanged; no new assumption or definition is introduced. Earlier structure-override attempts are rejected.

Canary01: all three public theorem axioms are standard, but the fixture probability normalization, Ici membership and Measure.ae_smul_measure qualification fail. Canary02 uses the actual ENNReal half-sum lemma and explicit namespace/membership. No target or fixture premise changes; failed elaboration sorryAx output is rejected.
