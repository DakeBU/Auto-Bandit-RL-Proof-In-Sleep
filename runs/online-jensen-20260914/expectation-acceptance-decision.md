# Accepted locally: signed expectation foundations only

Same-model sequential semantic decision: accepted-local for the seven frozen online-expectation-v1 interfaces and their shared definitions. Both finite positive/negative integrals are proved for integrable real functions, and finite compatibility uses the real Bochner positive-minus-negative identity. The nonnegative branch proves a zero negative part a.e.; the positive-infinite branch explicitly requires finite negative part. Total infinity-minus-infinity arithmetic is not used as a mathematical expectation.

Successful gates: expectation-leaf02; expectation-canary02 (8928jobs), all seven public targets and two canaries with only propext, Classical.choice, Quot.sound; seven native fences and frozen context; root/Tests9013jobs; full harness427tests/7existing skips;15registrytests; frontier shadow; final site756pages/9860Leanlinks; original full-root compiled exporter with5 asserted proof-value chains and source hash equal to the frozen check. The two-atom canary uses both signs. The nonconstant counting-integral canary has pointwise finite values and infinite integral. Counting measure is not a probability law; it is explicitly not a full Jensen source instance.

Parent Jensen v2 is UNPROVED. Its four exact headers are byte-identical to v1; only the context changed to import the actual shared expectation definitions rather than embedding duplicates. Original v1 fingerprints/type probes are retained. Both parent header probes deliberately fail at theorem bodies and are not compiled proofs. Affine minorant, arbitrary-convex barycenter, source negative-part finiteness, Theorem2.9 and its probability-law canaries remain required. Two later support/separation scratch candidates compile but are not public integration or accepted barycenter proofs.

Initial expectation and canary failures are retained; canary01's failed-elaboration sorryAx output is rejected. No foundation target/context weakened during repair. Individual tools enforce their own gates; semantic/role reviews are recorded conventions, not a single claimed runtime enforcing the entire workflow. No independent external review claimed. Chapter2 and total16-chapter Goal remain incomplete and active. Scoped PR delivery follows on PR124 exact ea1f873f754c88a30b0aa74e2308112cbb822f76; no merge or deployment. Worktree, source/graph caches and shared links retained.
