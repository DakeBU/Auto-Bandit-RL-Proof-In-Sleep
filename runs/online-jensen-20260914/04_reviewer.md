# Expectation foundation candidate review

Same-model sequential reviewer, GPT-6 Astra/medium, not independent external review. Seven frozen foundation headers and definition context are unchanged. Finite compatibility proves both lintegrals finite and uses the actual Bochner positive-minus-negative identity. Nonnegative reduction uses a.e. negative-part equality with zero. Infinite-positive result requires a finite negative part. None of these treats the total EReal infinity-minus-infinity value as a valid expectation.

Canary02 proves a signed two-atom integral and an infinite counting integral of a nonconstant pointwise finite function, with standard axioms for all seven library targets and both canaries. Counting measure is not a probability law; these are representation canaries only, explicitly not full Jensen source canaries. The Book entry marks the actual Theorem2.9 target planned/unproved and maps only existing foundation declarations to the shared registry.

The full Jensen parent is context-v2: all four headers byte-identical to v1, now importing the actual frozen shared expectation definitions. Both intentional failure-only header probes are type checks, not proof builds. Parent affine-minorant, arbitrary-convex barycenter, negative-part finiteness and full Jensen remain unproved. The support scratch candidate is not public acceptance. Foundation acceptance awaits root/Tests/full harness, full-root graph and final site gate. No whole source theorem or chapter completion is claimed.

All foundation gates now passed; see expectation-acceptance-decision.md. Full Jensen remains unproved, and geometric scratches remain unaccepted public dependencies.
