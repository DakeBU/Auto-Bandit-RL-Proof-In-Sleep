# Candidate semantic review

Same-model GPT-6 Astra / medium, sequential reviewer role; no independent external review. The four frozen v1 types match the reviewed contract. Necessity is proved from a local minimum and derivative along each feasible segment; sufficiency actually uses the accepted supporting-gradient bound. The finite-part ordering lemma rewrites values at both x and y, using explicit finiteness on V; neither infinity is treated as zero. The source terminal obtains point differentiation from the open neighborhood and composes both proved equivalences. Interior necessity invokes Fermat; sufficiency specializes the proved constrained criterion. No assumed one-step/minimality inequality replaces a proof.

Scope audit: hV is redundant with ConvexOn and nonempty is implied by hx; their unused-variable warnings do not indicate a missing source condition. They remain frozen explicit source premises. Convexity only on V is weaker than the source sufficient neighborhood-convexity premise, documented as a stronger result rather than an equivalence of assumptions. The boundary canary supplies convexity on the entire open neighborhood and restricts it with ConvexOn.subset; this checks source instantiability. Complete real inner-product generality includes the source finite-dimensional Euclidean space. No closedness, boundedness, minimizer existence or uniqueness is asserted.

The boundary canary has gradient1 at its minimum and a smaller value at infeasible one-half; the interior canary is a nonconstant quadratic on an open nonclosed feasible set. Both instantiate public terminals. Canary03 compiles with only propext, Classical.choice, Quot.sound. Earlier failed elaboration logs have sorryAx and are explicitly rejected, not accepted axiom evidence. Candidate is mathematically supported; acceptance still waits for combined root/Tests/full harness, original full graph and final site gate. Native fences enforce statement identity, while semantic reviews remain documented workflow conventions.

Combined root/Tests succeeded (9011 jobs). Full harness returned0 with 426 tests and7 existing skips. Final lean-verified site passed754pages/9845Leanlinks;14registrytests passed. Acceptance still pending original full-root graph checks.

Original full-root graph passed7 required proof-value checks and agrees with frozen source hash. Final decision accepted-local; see acceptance-decision.md.
