The existing OGD package covered only fixed learning rates. This change adds the actual variable-step projected gradient recursion and Theorem 2.13 for positive nonincreasing steps, retaining the negative terminal squared distance. The exact finite-diameter interface follows from a separate diameter-upper-bound theorem. Existing projection, gradient and fixed-step definitions are reused unchanged.

This PR is stacked on PR #117 at f1c623af48c8da6082a8cd93cad47ee2fbdf0351, which itself depends on unmerged PR #116. All Book mappings use the same Lean project and declaration registry; old OGD links remain. This completes only the decreasing-step package, not the remaining Chapter 2 convex analysis, subgradient or linearization results.

Validation: combined root/Tests/full harness passed (423 Python tests, seven existing skips), six frozen headers and the algorithm context match, and seven compiled proof-value dependency checks pass. The public two-round canary uses steps 1 and 1/2 with an active projection and terminal residual 9/16. Axioms are limited to propext, Classical.choice and Quot.sound. Site links/mappings pass (9788 Lean links). Earlier proof, canary, source-index and mapping failures are retained under runs/online-ch2-variable-20260914. This is local acceptance; remote CI, merge and deployment remain separate.
