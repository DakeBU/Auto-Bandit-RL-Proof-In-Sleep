Stabilized three parsed headers; starting the embedding bridge, followed by both source examples.

Leaf01: embedding iff and norm terminal elaborate. Affine arithmetic failed because the weighted-constant identity was not supplied to nlinarith. Leaf02 supplies congrArg (fun q => q*b) on a+c=1. Frozen targets unchanged.

Canary01 failed on a nonexistent real_inner shorthand and over-normalized EReal arithmetic. Canary02 uses the inspected RCLike.inner_apply API and rewrites concrete finite values before comparison. Failed-canary recovery sorryAx is not acceptance evidence. Core three theorem bodies compiled in leaf02 unchanged.

Canary02: explicit real-inner simp did not unfold the concrete inner instance under the function; norm_cast already closed the numerical comparison. Canary03 uses a definitionally reduced function equality and ring, and removes the redundant tactic. No theorem target changed.

Public-canary03 passes with standard axioms. Site01 rejected six teaching-route items (schema maximum four); Site02 keeps indicator iff/Theorem2.4/affine/norm as the concise route, with all other highlights and registry declarations retained. No schema limit bypass.
