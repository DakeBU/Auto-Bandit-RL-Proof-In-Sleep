# Candidate semantic review

Same GPT-6 Astra/medium sequential reviewer; not independent external review. Source v10 printedp10/PDF22 Examples2.5/2.6 are explicit main-text obligations, including the norm result whose proof is left as exercise. Three headers were frozen after explicit type elaboration. No target changed in arithmetic repair.

The bridge is an iff for actual real-valued f, whose effective domain is proved to be the whole space; EReal.toReal is used only on finite coercions. Affine example allows arbitrary slope z and constant b. Its convex inequality follows by the exact inner-product affine identity, with coefficient sum1 used for b. Norm example reuses pinned mathlib norm convexity and the same embedding bridge. General real spaces specialize to the finite-dimensional source, with no additional restriction. Both endpoints use the accepted shared epigraph definition; there is no second Book-specific convexity definition.

The canary must verify a nonzero-slope function with distinct values and strict epigraph nonmembership, a norm with positive nonzero values and midpoint zero, the zero affine edge case, and the reverse real embedding. Fresh successful public build and axiom output required; failed-canary recovery axioms do not count. Full combined root/Tests/harness, three native fences/context, actual compiled dependencies, source-qualified shared registry/site and delivery remain separate gates before accepted-local. General closure operations and later Chapter2 results remain required, and the total16-chapter Goal remains active.
