Local duplicate search found no matching full endpoint. Inspected existing OGD first_order and pinned ConvexOn.le_slope_of_hasDerivAt, comp_affineMap, DifferentiableAt.hasGradientAt, EReal.coe_toReal, Filter.Eventually.self_of_nhds. Closed-domain OGD reuse rejected as too restrictive. Real helper uses affine line differentiation; full endpoint uses finite-domain equivalence and a separate top case.
