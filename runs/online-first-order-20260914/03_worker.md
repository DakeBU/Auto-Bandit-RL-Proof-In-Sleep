Three typechecked headers/context stabilized. Prove neighborhood identity and real gradient bound, then full source endpoint; no target edits.

Leaf01: real supporting bound and full endpoint elaborate, but implicit inference for interior_subset selected an unintended set when directly nested under ne_of_lt. Leaf02 names the effective-domain membership explicitly. Targets/context unchanged.

Canary01 compiled all named witnesses with standard axioms but failed the final anonymous outside-point specialization because broad simp normalized EReal numerals before real subtraction. Canary02 rewrites the exact real subtraction only. No source or canary premise changed.

Public-canary02 passes with standard axioms. Site01 requires exactly three notation-primer entries; Site02 adds the comparator direction entry. No source or schema limit changed.

Verification-efficiency experiment: invoking the existing exporter through nested #eval dynamic imports returned0 and wrote JSON but emitted invalid-environment-extension PANIC messages. Reject this output entirely. It is not compilation/graph acceptance evidence. Retain script/log; original full exporter remains the gate. A standalone-main adapter may be audited separately against the full graph before any future use.

Standalone module export has no PANIC, but strict comparison with the full graph fails on generated congr_simp ownership under different import order. Retain the failed strict log and diagnostic report. Fixed public target nodes/edges match, but no replacement of the full-root exporter or canonical registry is adopted. The original full graph and four required chain checks pass. This bounded efficiency experiment is closed with a negative result.
