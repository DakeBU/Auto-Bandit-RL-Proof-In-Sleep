# Candidate semantic review

Same-model GPT-6 Astra/medium sequential review, not independent external review. The global terminal is byte-identical to parent online-jensen-v2, retaining unused measurable/Borel instances; the helper header is byte-identical to online-minorant-interior-v1. No closedness, lower semicontinuity or ambient interior enters the global target.

Interior branch: a finite graph point is not interior to the real epigraph because the vertical identity would have a local minimum. A nonzero supporting functional splits horizontally/vertically. The upward ray forces a nonpositive vertical coefficient. Zero would force the horizontal functional to attain a local maximum on the domain interior, hence vanish by Fermat, contradicting nonzero support. Division by the negative coefficient gives a global affine minorant; top values need no division argument.

Global branch: the nonempty convex domain has a relative interior witness p. Its affine span is supplied the Nonempty instance from p. Translation by p maps its direction space to the span. The pullback domain contains zero in its interior, and its real epigraph is convex by affine preimage. The helper's functional extends algebraically to E and is continuous in finite dimension. For each finite f(y), y lies in the span and y-p lies in its direction. Subtracting the value at p adjusts the affine constant correctly. For infinite f(y), the bound is automatic.

The public canary uses f(x,0)=x on x>0 and top elsewhere, with distinct finite values, a nonclosed domain in a proper line, and actual instantiation of the global target. Both public targets and two canary endpoints have only standard axioms. Acceptance is pending complete gates and site repair.

Final decision: accepted-local after all separately listed gates in acceptance-decision.md.
