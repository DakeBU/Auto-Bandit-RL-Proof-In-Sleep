# Next Chapter2 source audit (draft, no completion credit)

Printed p10/PDF22 reread: Examples2.5 affine inner-product plus constant and2.6 norms are required; all four closure bullets remain required despite exercise proofs. Mathlib locally inspected Analysis/Normed/Module/Convex.lean convexOn_univ_norm, Analysis/Convex/Function.lean ConvexOn.add, comp, comp_affineMap, sup. Existing EReal epigraph bridge will connect real instances to the same shared definition.

The first two and fourth closure bullets do not explicitly restrict function codomains, while Definition2.3 permits both infinities. Data/EReal/Operations.lean explicitly chooses bottom+top=bottom and zero*infinity=zero. Before freezing a general linear-combination theorem, audit whether this convention agrees with the source and supports the full epigraph claim. This is an unresolved semantics obligation, not an exclusion or an accepted real-only substitute. Affine composition and arbitrary pointwise supremum should be proved through epigraph preimages/intersections, preserving infinite values and empty-index cases. Monotone composition bullet explicitly has real-valued f and g. Next finite leaf candidates are the exact affine/norm examples; no future chapter write work has started.

## Concrete mixed-infinity counterexample to the naive Lean target

On the real line let f(x)=bottom at x=0 and top elsewhere, and g(x)=bottom at x=2 and top elsewhere. Their real-height epigraphs are respectively {0} times R and {2} times R, hence convex. With mathlib bottom-dominant addition, (f+g) equals bottom at 0 and 2, and top at 1. Thus (0,0) and (2,0) belong to its epigraph but their midpoint (1,0) does not. Even positive weights1,1 therefore refute the unrestricted naive EReal-add statement. This is a mathematical counterexample argument, not yet a compiled Lean counterexample.

Required resolution: explicitly define and source-audit the convex-analysis upper addition convention (top dominates mixed infinities), or record a source correction/definedness condition with the unrestricted ambiguity retained in the ledger. Do not silently use noBot as if it were printed in this bullet. The current indicator-addition theorem is unaffected because its noBot premise is explicitly printed. The general closure obligation remains required and under semantic repair.

## Proposed next exact finite examples (draft only)

Shared namespace BanditRL.OnlineConvex; import existing OnlineConvexExtended, mathlib norm convexity and inner-product basics. A reusable embedding bridge should state `IsConvexExtended (fun x => (f x : EReal)) ↔ ConvexOn ℝ Set.univ f` for every real-valued f. It must prove that the effective domain is all of E and convert only actual finite values. Example2.5 target: for every real inner-product space E, z:E and b:ℝ, `IsConvexExtended (fun x => ((inner ℝ z x + b : ℝ) : EReal))`. Example2.6 target: for every real normed space E, `IsConvexExtended (fun x => ((norm x : ℝ) : EReal))`. These general spaces instantiate the printed Euclidean setting. No boundedness, nonzero slope or positive norm assumptions are added. Nonzero-slope and nonzero-vector instances are required canaries, alongside the degenerate zero case. Dependency route: accepted embedding/epigraph bridge -> local real norm or affine identity -> exact extended-real examples. Headers require separate native freezes and explicit elaboration before any proof body.
