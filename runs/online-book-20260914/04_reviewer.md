# Semantic reviewer: preliminary candidate assessment

Same model in a separate role, not independent review. Source printed p4 requires prefix minimizer existence; the Lean hmem/hmin certificates express precisely that condition. Horizon zero is harmless and imposes no leader-zero requirement. The source's final-leader conclusion is preserved. Losses need not be convex. This auxiliary hindsight sequence must not be described as the causal FTL implementation. Proof uses induction, prefix optimality and final leader feasibility, not a regret hypothesis.

Decision: focused-compiled leaf; chapter acceptance withheld pending remaining terminals, public root canary, root/Tests/full harness/axioms/site checks. Initial omitted Real import is an ambient context defect recorded in repair v2.

Mean semantic review: n>0 explicit; all real observations allowed for decomposition/minimization; interval observation bounds required only for feasibility. Mean definition is the exact empirical average. Nonnegative n*(u-mean)^2 proves minimization instead of assuming it. These supply the actual leader certificates needed by Lemma1.2. Full Chapter1 acceptance remains pending.

Theorem1.3 candidate semantic review: prediction is 1/2 at t=0 and mean of exactly t previous observations thereafter; prefix theorem rules out future dependence. Source argmin is represented by the empirical mean, with feasibility and global minimality proved. Per-round bound uses interval feasibility and real mean update. Final proof invokes Lemma1.2 and the mathlib harmonic upper bound. Result is focused-compiled, not chapter-accepted. Module canary and axioms are separate evidence; public root, stochastic motivation, no-regret formal semantics and full chapter site/gates remain pending.

Stochastic semantic audit: identities assume square-integrability on a probability space. Random prediction identity explicitly assumes independence from current target; this cannot substitute for deriving independence from IID history. NoRegret uses an eventual upper bound for every positive epsilon (limsup interpretation); it does not claim the signed regret is nonnegative or convergent. This formal interpretation is disclosed in regret-contract.md. Neither adapter is a complete source-game verification by itself.

The actual no-regret theorem quantifies all infinite interval-valued target sequences and uses one prediction function for every comparator. No stochastic assumption enters it. The independent-prediction bridge assumes independent measurable observations, not prediction independence; it derives the latter from the actual strict-past mean. Identical distribution is unnecessary for this bridge, but required for common variance in source cumulative benchmark. Remaining source obligations: bounded integrability, mean/variance benchmark accumulation and its nonnegativity; then complete source inventory review and full public gates.

IID source review: expected cumulative loss is an integral of a finite sum; finite-sum exchange is justified by derived square integrability. Exact excess equals a sum of squared mean-estimation errors. Common law supplies a single variance benchmark; actual predictor uses only the strict past. source_mean_optimal proves the expected mean is in [0,1] and attains variance. No arbitrary future-informed predictor claim. Remaining chapter audit must check normalized Eq1.2 and the source's general information-semantics statement, plus public probabilistic canary and full gates before acceptance.

General-policy source audit: policy domain is precisely the finite past tuple, including empty history at round zero. No future-sequence input. Measurability and bounded predictions are explicit and sufficient for expected-loss statements. The source deterministic guessing game is covered; no claim about a separately randomized strategy absent an independent seed model. Probability canary has distinct outcomes and positive mass on both. Public-root canary and full gates remain required.

## Final candidate evidence (supersedes earlier pending-session entries)

All seven required source groups have compiled interfaces. Public root/Tests passed 8998 jobs; all 24 statement fences and nine context prefixes match. The final graph includes empiricalMean_unique and all seven required direct proof-value dependency edges; no sorryAx was found. Final site validation passed (9779 Lean links, 130 highlights, 18 Mermaid diagrams). Same-model semantic review found no remaining mathematical obligation within the enumerated Chapter 1 scope; this is not independent external review.

Acceptance is withheld: the complete local harness failed with nine errors (eight OpenSSH temporary-key errors and one child termination timeout), with seven other tests skipped. No test was removed or mocked. GitHub reads work, but the create-blob write was rejected because approval is required and this environment uses approval policy never. No new remote branch or PR exists. WSL is not installed and Docker is not on PATH; no local Linux gate is available from the inspected environment. The protected temporary keys and shared links are retained. The total Goal remains active; Chapter 2 proving has not started.

Latest decision: see acceptance-decision.md. Full-gate-02 passed423tests (7existing skips), resumed contract and site checks passed; Chapter1 accepted-local, PR117 open. Earlier failures and pending states are historical. Goal remains active for all16chapters.
