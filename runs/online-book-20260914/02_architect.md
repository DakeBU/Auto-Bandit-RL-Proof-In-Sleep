# Architect and repair

Induct on horizon. The zero horizon is an empty sum. At successor, cancel the common last loss, apply the prefix induction, and compare its final leader to the next leader using prefix optimality and membership. n=0 is handled separately, avoiding an invented leader-zero minimizer assumption.

Attempt 01 failed: import omitted Real.Basic; Lean autoImplicit treated the glyph Real as a free type, lacking AddCommMonoid. No candidate compiled. Added explicit real-number import; v2 captures the same textual header with corrected ambient imports. The v1 textual fence is retained as failed-context evidence. This is a context repair, not proof success or theorem weakening. Freeze the complete pre-proof prefix hash to detect ambient context drift.
