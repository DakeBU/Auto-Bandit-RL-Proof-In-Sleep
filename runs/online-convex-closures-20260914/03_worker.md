Three typechecked frozen headers stabilized. Implement exact preimage/intersection/global-composition proofs with no terminal edits.

Public-canary01 passes with standard axioms. Site01 rejects external Mathlib names as teaching dependencies because this registry surface accepts project declarations. Site02 links the shared epigraph definition while keeping the actual external API in the proof explanation and compiled graph evidence. No fake project wrappers or validator bypass.

Site02 check rejected nine notes in a chapter capped at six. Site03 splits a dedicated closure teaching page referencing the same graph, retains every prior highlight/link, but its new worked example used setup instead of the required intro field. Site04 corrects that schema field. No limits are raised.

Full-gate01 failed one registry assertion: the running unittest process loaded the pre-split test code while the working-tree chapter mapping was subsequently split. The final focused registry run passes12tests. This mixed-snapshot full gate is rejected, not counted as acceptance. Freeze current source/metadata and rerun full-gate02; do not edit tests or site metadata during it. Site05 rebuilds the final wording that labels closures as implications rather than the inherited equivalence wording.
