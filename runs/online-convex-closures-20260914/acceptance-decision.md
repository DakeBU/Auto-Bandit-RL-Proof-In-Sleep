# Accepted locally: three convexity closure operations

Same-model sequential reviewer decision: accepted-local for the three v1 frozen endpoints. See04_reviewer.md for source assumptions, quantifiers, information and semantic review. These close three exact source bullets, not the entire group of four, Chapter2 or the whole-book Goal.

Successful gates: leaf01 module and public-canary01 (8924 jobs, named public statements and only standard axioms); three native fences and unchanged import context; combined root/Tests and full-gate02 tools/bandit.py check (424 tests,7 existing skips); read-only frontier shadow without mutation/mismatch; final shared registry12tests; final site05 (749 pages,9818 Lean source links). All successful processes returned0. Full-gate01 failed a stale in-memory registry assertion during the teaching-page split and is explicitly rejected. Final source/metadata were held fixed during full-gate02. Site01/02/03 failures and their repairs remain recorded; site04/05 pass.

Compiled native graph export and scoped extraction passed:5owned/generated nodes,127boundary nodes,216edges and3asserted proof-value dependencies. Affine precomposition uses Convex.affine_preimage, supremum uses convex_iInter, monotone composition uses the accepted finite embedding iff. Graph and frozen report share the same module LF SHA256; no sorryAx dependency. The full ignored185MB graph is retained locally; scoped evidence and export hash are versioned.

No noBot/nonempty-family/injective-map/convex-image premises were added. Canary instances include translated infinite-valued constraints, constant noninjective affine maps, two different constraint indicators and distinct supremum values, empty family, and nonconstant monotone norm composition. Metadata uses a separate teaching page to respect existing note limits while all Books retain one Lean registry and old links.

The fourth nonnegative-combination source bullet remains required, with explicit upper-addition source interpretation and eight draft typechecked targets awaiting their own contract and proofs. Later Chapter2 and Chapters3-16 remain incomplete. Stage reviews are documented conventions; native checks enforce their individual gates, not a claimed single runtime for the entire paper workflow. No independent external review claimed.

Delivery: stacked on PR120 exact4bf9714; scoped commit/PR follows. Canonical main/live unchanged. PR119 CI passed; PR120 build was still pending at the last snapshot. Worktree retained for ongoing whole-book work; shared packages and protected paths preserved.
