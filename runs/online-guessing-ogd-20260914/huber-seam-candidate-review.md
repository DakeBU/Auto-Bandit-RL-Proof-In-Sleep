# Huber seam foundation candidate

The single frozen target joins two real functions at c when their values and derivatives agree there. Its proof restricts the two derivatives to Iic c and Ioi c, identifies the conditional function on those sets, and combines the within-derivatives on their union, univ. The value equality at c is used explicitly; neither boundary is silently omitted.

The deliberate header probe failed its body only. Leaf01 compiled the actual proof. Canary01 instantiates the nontrivial quadratic/affine join at c=1 with derivative 1, and both public-name probe and canary report only propext, Classical.choice and Quot.sound. The native frozen check is separate header evidence, not a compiler. Saved .lean.txt files preserve both candidate and canary from ignored scratch paths.

Same-model sequential review accepts this scratch candidate as reusable foundation growth. It is not yet a public library declaration and does not satisfy the source Example2.15 endpoint. Public integration, exact scalar Huber loss, both seams, convexity, vector gradient, bounded-feature gradient estimate, causal unconstrained OGD guarantee and full acceptance gates remain required. No Chapter2 acceptance follows from this leaf.
