# Candidate source semantic review

Same-model GPT-6 Astra/medium sequential reviewer, not independent external review. Exact Theorem2.9 and negative-part headers have identical bytes/native hashes to v2/v1. The v3 context imports the actual accepted shared dependencies. The source uses Euclidean random vectors; finite-dimensional normed spaces generalize that model. No closedness, continuity, full dimension or integrability of the loss is assumed.

The probability law is nonzero, so a.e. domain membership gives a finite point. The affine minorant supplies an integrable real lower bound because continuous linear maps preserve input integrability and constants are integrable under a probability law. Negation reverses the lower bound; monotonicity of EReal.toENNReal and lintegrals proves finite negative part. Measurability hypotheses are unused in that subproof but retained exactly.

The endpoint first splits on the actual positiveIntegral. If it is top, the proven finite negative part permits signedExpectation_eq_top; no infinity-minus-infinity default is used. Otherwise the measurable real representative Y of f(X) equals it after coercion almost surely by noBot and a.e. domain membership. Finite positive/negative integrals imply integrability of max(Y,0) and max(-Y,0), whose difference is Y. Thus (X,Y) is an integrable epigraph-valued vector. The shared arbitrary-convex barycenter puts its true integral in that same epigraph, including nonclosed domains. integral_pair and a.e. signed-expectation compatibility identify the exact required terminal.

Public-canary02 instantiates the actual source theorem in three settings. Finite equal atoms1,3 give mean2 and expected square5. A geometric law with parameter3/4 and X(n)=2^n has summable weighted input but constant3/4 weighted square, hence infinite expected loss. It is a probability law with finite sample losses. The nonclosed case uses the varying loss x on the positive ray in a proper line, top elsewhere, proves measurability/domain nonclosedness and instantiates the source endpoint. Both public targets and four audited canary endpoints have only standard axioms. Initial rejected canary01 sorryAx is not accepted evidence.

Native header/context checks and public canary pass. Full root/Tests/harness, actual dependency graph and final site remain separately required. Theorem2.9 is not Chapter2 or whole-book completion. Old expectation-only registry assertion was correctly restricted to its foundation page; the source theorem has its own shared registry test, no duplicated proof tree. Site preflight repairs preserve old numeric metadata and use local teaching-route highlights with cross-page dependencies in the shared graph.

Final decision: accepted-local for the exact source endpoint and finite-negative-part prerequisite after all separately recorded gates. See acceptance-decision.md.
