# Next source example candidate

Example2.10 source printedp12/PDF24 reread; seven exact targets and actual recursive statistic/selector definitions were frozen before proof search. The header probe had exactly seven intentional body failures and no type errors, not seven compiled theorems. FTL leaf01 and canary01 genuinely compile. Native fences and definition/context hash pass unchanged. Nine audited theorem/canary endpoints have only standard axioms.

The predictor computes the sum of exactly t previous coefficients by a recursive update. Its strictly past dependence is proved for any two coefficient streams. With feasible initial x0 it remains in[-1,1], and for every time it minimizes the cumulative previous linear losses over[-1,1]. At t=0 all cumulative losses vanish, so arbitrary x0 is allowed. Later tied coefficient0 selects-1, a valid minimizer. On the source alternating stream, every positive-time prefix coefficient is nonzero (plus/minus1/2), yielding the stated alternating predictions and per-round loss1. The initial loss is -x0/2. Induction yields exact regret T-1-x0/2 versus0 for T>=1, then x0<=1 gives T-3/2. No one-step regret or future-loss minimizer is assumed.

Canary uses x0=1/3 and six rounds: actual predictions begin1/3,1,-1 and total regret29/6. This remains a scratch-only candidate, not accepted/public. Required next steps are public root integration, exact source Book mapping, canaries/fences/axioms, graph, full combined root/Tests/harness and site review. Current public Jensen gate sources were not changed during this work. Same-model GPT-6 Astra/medium sequential roles, not independent external review.
