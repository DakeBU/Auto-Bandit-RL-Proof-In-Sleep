# Extended-real convexity contract v2

Source arXiv1912.13213v10 (June21 2026), SHA256 cef4edfa97a6e063e53e9c532717c50aa156e5bc782ea49f969b3385011a1b17. Printed9-10/PDF21-22 reread. Scope: Definition2.2, Definition2.3, Theorem2.4 and the intervening effective-domain/constraint-indicator consequences. Example2.5/2.6, the four closure operations, Theorems2.7-2.9 and later Chapter2 results remain required subsequent targets, not excluded. This packet does not finish Chapter2.

EReal permits both infinities for Definition2.3. effectiveDomain is exactly f(x)<+infinity, so -infinity belongs to the domain. The epigraph is a subset of E times REAL heights, not E times EReal. The constraint indicator is zero on V and +infinity outside; mathlib Set.indicator would be a different definition and is not used. Empty sets and empty domains are allowed. IsConvexExtended is epigraph convexity, not a redefinition by Jensen's inequality.

Theorem2.4 assumes f never takes -infinity and assumes convex effective domain, exactly as printed (the separate indicator-addition result also retains its printed noBot premise). Its RHS quantifies only points in that domain and strictly interior scalar weights. Real-valued toReal conversion is used only with explicit finite-value proofs, and its epigraph bridge is proved; no top or bottom is silently treated as zero. Supporting lemmas convex_effectiveDomain and convex_indicator_iff retain the full stated scope. The real-vector-space interfaces instantiate finite-dimensional real Euclidean spaces without new library/toolchain dependencies.

Seven exact headers and the four definition bodies are frozen before implementation. Source statement fingerprint is the native per-header statement hash; source bytes are pinned by PDF hash. Initial DAG: EReal finite coercion/order API -> realEpigraph_toReal -> convexExtended_iff_toReal -> Theorem2.4 using mathlib convexOn_iff_forall_pos. Definition2.2 uses convex_iff_forall_pos. Epigraph convexity -> convex effective domain; indicator domain identity + product epigraph -> indicator convexity equivalence. Initial ready leaves: definition2.2/domain/indicator domain; helper context fixed.

Allowed edits: OnlineConvexExtended module, scoped contracts/canary/run/obligations, later shared root/Tests/registry. Preserve earlier OGD contracts and global SGB frontier. Same GPT-6 Astra/medium model fulfills sequential director/architect/worker/reviewer roles; no independent external review claimed. Status draft→stabilized for this packet, not the still-under-review rest of Chapter2.

Repair v2: v1 used the reserved parser token lambda as a binder in Definition2.2/Theorem2.4; it was not a well-formed Lean statement. Only that bound variable and its references change to theta. No quantifier, hypothesis, scalar interval or conclusion changes. v1 files and failed leaf01.log remain. v2 header-type probe must report only the seven intentional unproved-body failures before this stabilization is relied upon.

Source-scope extension, frozen before its proof: convex_add_indicator covers the explicit unnumbered printed-p10 statement for noBot f and convex V. Its epigraph is the intersection of the original epigraph with V times real heights. This eighth header does not change the previous seven or their context. Other four general closure operations and Examples2.5/2.6 remain required subsequent work.
