# Three of four source closure operations, v1

Orabona arXiv1912.13213v10 printedp10/PDF22, SHA256 cef4edfa97a6e063e53e9c532717c50aa156e5bc782ea49f969b3385011a1b17. The affine precomposition, monotone real convex composition and pointwise supremum bullets are required main-text results, despite proofs left as exercises. This packet covers these three exact operations; the nonnegative linear-combination bullet remains a required separate upper-addition contract, not excluded or counted complete.

Affine precomposition accepts an arbitrary affine map between real vector spaces, not just translations or injective maps. Input f permits both infinities and empty domain. Supremum quantifies an arbitrary indexed family, with no nonempty index, bounded-family or noBot assumption. Thus empty supremum is bottom and genuine infinite values remain covered. Monotone composition follows the source's explicitly real-valued f and g with global convexity and global nondecreasing g. No convex-image assumption is added.

Three exact Lean headers and import context are frozen before proof. The explicit header probe has exactly three deliberate body failures and no other errors; this is target-type evidence only. Real-space generality includes the source finite-dimensional Euclidean instances. The same shared epigraph definition and declaration registry are used.

DAG: accepted convexity definitions + affine product-map preimage API -> affine endpoint; complete-lattice supremum order + convex_iInter -> supremum endpoint; accepted real embedding iff + monotonicity and convex inequalities -> monotone composition endpoint. All three leaves have ready dependencies. Terminal edits prohibited during proof repair; new assumptions or conclusions require a new reviewed contract version. Single lower route, sequential GPT-6 Astra/medium roles, not independent review. Allowed edits: this module/canary, scoped evidence/contracts, shared root and source-qualified registry integration. Earlier package contracts and SGB global frontier remain unchanged.
