Imports and the three named sections are frozen as in integration-header-types.log and the initial integration draft. SupportEquality has CompleteSpace, not FiniteDimensional; FiniteSupport has finite dimension; Barycenter has finite dimension and the source-v2 measurable/Borel context. Universe naming only changes bound-variable spelling. All three headers byte-identical to their earlier contracts. No target assumption changed.
