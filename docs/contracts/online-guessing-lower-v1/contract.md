# Example2.14 comparative-remark lower witness v1

Semantic review: the existing explicit2sqrtT upper guarantee does not itself prove the source prose claim that this horizon-selected OGD is suboptimal compared with the Chapter1 logarithmic guarantee. Keep those six original terminals fixed; add a separate required lower-witness obligation rather than infer a lower bound from an upper bound.

Use the same shared OGD with initial1, all labels0, horizonT=(2n)^2, n>=1 and exactly the source step1/(2sqrtT)=1/(4n). Its true trajectory is(1-1/(2n))^t. For t<n, Bernoulli gives trajectory>=1/2; hence each of the first n square losses>=1/4. Comparator0 has zero loss, so regret>=n/4=sqrtT/8 on arbitrarily large square horizons. This concretely supports the comparative claim; it is not a new printed numerical lower bound attributed to the author. No change to source loss, algorithm, feasible set or step schedule.

Three headers/context frozen before proof search. This is a scratch-only required comparative refinement while the six-target upper-guarantee package is gated. Until integrated and verified, the source comparison remains pending; do not declare Example2.14 wholly closed solely from its upper bound. Same-model GPT-6 Astra/medium sequential roles, not independent external review.
