# Supporting-gradient Theorem2.7, v1

Source Orabona arXiv1912.13213v10 printedp11/PDF23 reread, SHA256cef4edfa97a6e063e53e9c532717c50aa156e5bc782ea49f969b3385011a1b17. Exact endpoint: extended-real convex f never assumes bottom, x belongs to the interior effective domain, differentiable at x, and for every y, f(x)+inner(gradient at x,y-x)<=f(y). No closed/bounded domain or everywhere differentiability condition is added. Complete real inner-product spaces include all source finite-dimensional Euclidean spaces.

Differentiability/gradient are expressed through z->(f z).toReal. This representation is valid only locally: finitePart_eventually proves its embedding agrees with f on a neighborhood of x using the interior-domain/noBot premises. The public endpoint keeps all y, including outside-domain positive infinity. It does not treat those infinite values as zero. Ordinary addition is unambiguous here because f(x) and the gradient term are proved finite.

Three exact headers and full context frozen after three deliberate-body-only type errors. DAG: open interior/finite EReal coercions -> neighborhood identity; real convex affine-line derivative API -> convex_gradient_lower_bound; both helpers plus the accepted epigraph finite-domain equivalence -> theorem_2_7, with an explicit outside-domain top case. The general real helper is reusable foundation connected to the fixed source terminal, not independent completion credit. Prior OGD first_order requires a closed Domain structure and covers only real losses, so it cannot substitute for this full endpoint.

Semantic review retains every source hypothesis and conclusion scope. Nonconstant restricted-domain canary must prove local differentiability, a nonzero gradient and a top-valued outside comparator. Frozen targets/context may not change in proof repair; new versions require review. Same-model GPT-6 Astra/medium sequential roles, not external review. Allowed edits: new module/canary/scoped evidence and shared root/registry integration. All other Chapter2 obligations including2.8/2.9 remain required; no whole-chapter completion.
