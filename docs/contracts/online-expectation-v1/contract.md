# Signed expectation foundation v1

Seven exact headers and the three-definition/context prefix frozen after seven deliberate body-only errors. Definitions use positive and negative ENNReal lintegrals, with their difference embedded in EReal. Mathematical use requires at least one finite part; the Jensen parent requires a separately proved finite negative part. This module must prove compatibility with actual Bochner integrals for integrable real functions, the a.e. nonnegative reduction, and a positive-infinite/finite-negative expectation case. It does not declare infinity-minus-infinity to be a valid expectation. Arbitrary measures and measurable sample spaces; no probability assumption needed for these foundations.

DAG and source connection: see ../online-jensen-v2/contract.md. The source terminal does not assume composite integrability, so finite compatibility is only one branch. Existing mathlib positive/negative part and Bochner identities are reused directly. No toolchain/dependency changes. Scope edits new module/canary/evidence and shared roots/Book mapping, never a separate project. Same-model sequential review. Frozen theorem types and definition context cannot change during repair. Foundation acceptance is not Theorem2.9 acceptance.
