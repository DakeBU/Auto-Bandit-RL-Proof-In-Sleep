# Promote the reviewed static learner to the shared library

Target: integrate the frozen repaired static Algorithms 1/2 and complete
finite-horizon expected-regret chain, with the accepted noisy full-learner
canary. Source/version/assumptions remain those in MULTI-AGENT-CONTRACT.md and
the ten component review receipts. This is an integration node, not a new
source repair or permission to weaken a statement.

Current upstream main was fetched explicitly and is b3201f8, already an
ancestor of the research branch. Canonical and Online Learning checkouts are
untouched. The existing pinned Lake project and dependency junction are reused.

The deterministic promote_musical_chairs.py checks that, after deleting import
lines, each reviewed scratch layer has an unchanged preceding prefix. It
extracts nine modules for exploration, reward concentration, collision counts,
population estimation, ranking, learned kernels, marginals, staged regret and
complete rewards. Twelve canary namespace blocks move to one Tests module
which imports the combined BanditRLProof root. Declaration namespaces and proof
bodies stay unchanged; no production proof refers to a canary.

The extraction receipt binds all ten source snapshots and ten generated files.
Independent scan comparison finds all 391 declarations with exactly unchanged
names/statements: 298 production and 93 test declarations. Named instances are
retained in their source blocks and will also be audited through the compiled
public root/test environment. Production uses the existing coordination/time/
regret modules, product probability APIs and Mathlib facts already recorded by
the component worklogs. No new generic mathematical lemma is introduced here.

Mandatory acceptance: focused module build; combined root and Tests; no
placeholder or new-axiom closure; independent extraction/promotion review;
attributed mathematical reader and exact folded production proofs; shared
declaration/source/receipt mapping; contribution contract; full harness and
site/books checks with fresh Lean verification. Code generation is not evidence
that these gates passed. Record actual terminal results before acceptance.

Graph delta: integration-node for the Lean graph; update the partial multi-agent
Overview and results; Functor none-found-with-reason because no new certified
cross-topic transport is introduced. Other book routes have no change because
this is an Extended Topic, but their combined Lean compatibility remains a gate.
The recent-source comparison and ICLR evidence remain open; topic_complete
stays false. No main merge, anonymous snapshot update or deployment is implied.
