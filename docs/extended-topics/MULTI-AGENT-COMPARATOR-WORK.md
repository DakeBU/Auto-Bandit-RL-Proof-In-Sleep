# Global comparator and total-horizon phase assembly

Source: pinned Rosenski--Shamir--Szlak static Theorem 1 / supplement A.1,
with the existing frozen all-player exploration-budget repair. Target the
missing all-history true-top comparator bound and actual exploration/coordination
phase splice, preserving every initial and bad-event term.

Owner: runs/extended-topics-20260920/MusicalChairsComparatorPrototype.lean.
Semantic fingerprint: arbitrary actual simultaneous action vectors, collision-free
players, nonnegative bounded true means, top-n reference, all finite horizons.
No good-event or common-candidate premise for the global per-round bound.

Reuse search: actual topArms/scoreOrder and top-before-unselected theorem;
Finset minimum image, cardinality of set differences and finite sum splitting;
existing trajectory/action and reviewed conditional coordination expectation.
A reusable ordered-set maximum-sum lemma will serve both bad-path and exploration
cost bounds. Pairing may be avoided by the minimum selected-score threshold.

Reader and semantic plan: exact folded scratch, proof/assumptions/constants and
source deltas, distinct blind decoder/source reviewer plus axiom audit. Keep
scratch acceptance separate from public root/Tests/harness/site gates. Lean graph
adds comparator/phase producer edges; public Overview/Functor graphs remain
unchanged until extraction, with no unsupported discovery claim.

## Constructed endpoint and validation

The minimum-selected-score argument proves the reusable ordered finite-set sum
maximum. Actual collision-free earned-arm image and its injectivity/cardinality
connect this to all-action 0<=roundPseudoRegret<=n. No candidate correctness or
state invariant appears in this producer.

The actual chronological learnerAction splices exploration and the existing
coordination trajectory. Its prefix theorem and exact learnerRegret split cover
H=0, H<S, H=S and H>S. The initial cost is n*min(H,S); total pathwise regret is
[0,nH]. Finite action projections prove integrability without weakening signed
real sums. Actual good/bad restricted integrals yield
E[R_H]<=min(nH,n*min(H,S)+8*n^2+delta*nH). Source wrappers supply probability and
mean/comparator premises from actual numeric-gap exploration producers. Coarse
source residual, 2*exp(2) comparison, normalized conditional version and
delta=1/H (H>=2) residual n are proved.

Focused scratch compiled cleanly; separate axiom audit exited 0 and parsed all
42 new names (33 theorems, nine definitions; seven canary theorems). Only propext,
Classical.choice and Quot.sound. Exact folded reader and unchanged marginal
context verified. Exploration future-coordinate truncation and a nonzero
two-phase transient are deterministic diagnostics, not full stochastic runs.

Compiler repairs concerned score equality orientation, explicit sample-space and
finite-projection types, restriction-set inference, natural minimum casts,
concrete finite-index inequalities and ENNReal/real constant arithmetic. A finite
projection type annotation resolved an elaboration heartbeat limit; the limit
was not increased. No source assumptions or endpoints were weakened. An exploratory
API #check file had unknown-name diagnostics and is not validation evidence.

## Remaining source-model and acceptance route

Add independent post-exploration bounded arm reward arrays to the actual composed
law, define selected rewards with the separate collision bit and complete local
feedback trace, and prove prefix causality of that trace. Fresh future arrays
permit conditional-on-history mean identities in the continuation. Exploration
requires its actual action/reward independence and preserved marginal. Combine
them only for UNCONDITIONAL expected realized=pseudo regret; do not transfer that
equality to the reward-dependent exploration good event.

Run the frozen full noisy unknown-N Bernoulli learner canary after this model is
complete. Extract reviewed scratch into shared public modules with root, Tests,
full harness, declaration/axiom/dependency and site gates. Recent-source review
and all-topic ICLR evidence remain pending. No topic or program completion,
public integration or live publication is claimed.

## Independent acceptance

Distinct blind decoder /root/blind_decoder_b and source reviewer
/root/source_reviewer_a agree on all seven slots, all seven canaries and the
remaining source-model limits. Verdict: accepted-with-explicit-delta for the
staged-action pseudo-regret endpoints. Full realized reward process, local
feedback trace, noisy learner canary and production acceptance remain excluded.
Exact review hashes and all 42 declarations are bound in
runs/extended-topics-20260920/multi-agent-comparator-review.json.
