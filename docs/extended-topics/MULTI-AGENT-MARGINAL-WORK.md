# Actual finite path marginal and performance bridge

Source: pinned Rosenski--Shamir--Szlak static coordination phase and supplement
A.1; retain frozen repaired source contract. Consume the reviewed actual iid
learned-candidate path kernel, not an assumed state law.

Retrieve Fin.snocEquiv, Fin.prod_univ_castSucc, finite sum reindexing and PMF
map/bind laws. Prove iid prefix/last-draw decomposition, trajectory pushforward
stateLaw equality and current-draw joint law; then transport actual pathwise
regret to the accepted coordination bound. Generic finite PMF lemmas should be
reusable within the same shared project. Preserve the existing trajectory and
actual action function; no replacement process or future feedback.

Scratch module: runs/extended-topics-20260920/MusicalChairsMarginalPrototype.lean.
Requires focused compile, axiom audit, exact reader fold and distinct blind/source
review before acceptance. Full learner phase splice/global comparator/future
reward and joint production gates remain separate.

## Constructed chain and execution evidence

The actual iid PMF now has snoc decomposition and all-prefix projection laws.
Those identify the existing trajectory's terminal and intermediate state with
stateLaw, then its state/current-draw and played-action marginals. Finite map/bind
real expectation transport identifies the actual cumulative path deficit with
the already accepted coordination regret sum, yielding <=8*n^2. No positive-part
replacement is used.

The actual exploration compProd restricted to measurable E contained in the
actual good event has path marginal P_explore(E) times the common-set iid law.
An exact real integral identity and positive finite denominator give the
normalized event coordination expectation <=8*n^2. The source-facing theorem
derives all common-set/cardinality/mean/measurability/positive-G premises from
primitive numeric-gap and arbitrary bounded arm-law assumptions.

Focused scratch and separate axiom audit both exited 0. Audit parsed all 34 new
named declarations: 32 scanned (29 theorems, three definitions) and two discrete
state instances. Only propext, Classical.choice and Quot.sound. Exact reader
fold and unchanged prior handoff context verified. Six canary theorems include
positive probability 1/16 and actual real regret 5/4 for an initial collision
followed by separation; later ignored draws are separately checked.

Compiler repairs: explicit constant dependent families for Fin.snoc, explicit
Fin cast equality for prefix projection, reconciling decidability instances in
a PMF event sum, correct map-toMeasure API argument order, discrete state
measurability, finite-sum distribution and ENNReal inverse-power arithmetic. No
source assumption or conclusion was weakened to repair elaboration.

## Next source endpoint route

For arbitrary played actions, form the finite image of collision-free players;
its cardinality is at most n and the action map is injective there. Prove that
the actual n highest nonnegative means dominate every such subset, using the
ranked order and pairing elements outside the top set with missing top arms.
This supplies global 0<=roundRegret<=n, including bad histories.

Splice actual exploration actions and existing continuationAction at the exact
natural exploration length. For total horizon H<S, only the observed exploration
prefix contributes; the unused latent future exploration sample must not become
an action input. For H>=S, the learned configuration uses completed exploration
only. Prove sum decomposition, exploration cost n*min(H,S), and apply the current
conditional coordination theorem. Bound the remaining bad-event contribution
by delta*n*H, preserving both the all-horizon n*H bound and the initial cost.
Future realized reward equality remains a separate fresh-reward-law obligation.

No new public-root/Tests/full-harness/site gate is claimed for this scratch.

## Independent acceptance

The distinct blind reconstruction by /root/blind_decoder_b and source comparison
by /root/source_reviewer_a agree on the actual law, indexing, signed real
expectation, event normalization and canaries. The source verdict is
accepted-with-explicit-delta for this bounded scratch component. Hash bindings,
all 34 names and execution evidence are recorded in
runs/extended-topics-20260920/multi-agent-marginal-review.json. Full learner
regret, future rewards, public acceptance and topic completion remain excluded.
