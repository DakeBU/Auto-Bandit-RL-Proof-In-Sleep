# Current local Chapter 14 browser checkpoint

Build snapshot: clean commit 24aa742, generated at 2026-09-05T07:27:24+00:00,
633 modules, 8436 declarations, zero placeholders. Static check passed:
687 pages, 9235 Lean source links, valid internal links/anchors, mathematical
fallbacks and Pages workflow. This is a local build, not a remote deployment.

An isolated loopback-only preview served tmp/ch14-site; the old website/_site
server was not changed. The in-app browser showed the current Chapter 14 title,
separate compiled-leaves and partial-whole-chapter statuses, source boundaries,
and new unique-decoding and arithmetic-payload entries. The default desktop
viewport screenshot showed readable heading and status prose without overlap.

The unique-decoding entry's target was checked directly:
modules/banditrlproof-lowerbounds-prefixcodeconstruction/index.html#decl-8de6b7c62c08.
It selects the expected theorem, renders its full finite/injective/unique-decoding
signature and equal-symbol-length conclusion, and offers a scrollable statement.
The arithmetic payload row explicitly records the b5e21b8 gate and actual named
code invariant. Singleton, uniform-cardinality, exact-real and finite-alphabet
qualifications remain visible in the Chapter 14 DOM.

Interaction limitation: clicking the unique-decoding link once did not produce
an observed URL change or new tab. Direct navigation to its observed href worked.
Thus URL/anchor/content verification passed, but click navigation has not been
certified. No mobile layout, full interaction sweep, independent review or
public deployment claim is made by this checkpoint.
