{
  "schema_version": 5,
  "suite_id": "ABRL-TARGET-DRIFT-V2",
  "status": "result_free_agent_image_and_action_interface_candidates",
  "controller": {
    "entrypoint": ["python3", "/usr/local/bin/abrl-agent-pid1"],
    "pid_namespace_requirement": "controller is PID 1; --init is forbidden",
    "control_channel": "Docker stdin remains open for the adapter lifetime",
    "control_loss_action": "PID 1 terminates the direct child and exits",
    "kernel_boundary": "Linux removes all remaining processes in a PID namespace when PID 1 exits"
  },
  "required_probe": {
    "id": "host_control_loss_reaps_pid_namespace_v1",
    "attack": "the adapter fixture creates a new session and process group, then the host Docker client is killed without graceful cleanup",
    "success": "the labeled container disappears and the escaped descendant heartbeat stops",
    "binding": "report hashes the Docker executable/identity, image digest, in-image controller bytes, and exact run command"
  },
  "provider_client_image_candidate": {
    "base": "the exact cache-complete checker image built from the common pre-audit workspace",
    "source_lock": "the native Linux Codex registry package is version-, URL-, SRI-, and SHA-512-locked, then reduced to four regular allowlisted members before Docker sees it",
    "contents": "the inherited offline Lean toolchain/cache, exact Codex executable/bundled bwrap/rg, the target-drift adapter, and the PID-1 controller",
    "host_user_namespace_boundary": "Docker applies the hash-bound abrl-target-drift-codex AppArmor profile to the entire non-root agent-container process tree. Its sole AppArmor rule grants userns so the trusted native Codex client can launch its nested bubblewrap user, PID, and network namespaces; it supplies no Linux capability, while Docker still enforces its read-only root, capability drop, no-new-privileges, resource, and outer-network boundaries",
    "required_result_free_probes": [
      "in-image Codex/Lean/Lake/cache/controller/adapter bytes match their build ledgers",
      "the evaluated child reports the exact abrl-target-drift-codex AppArmor profile inherited by the non-root agent-container process tree before any workspace, network, or credential observation is accepted",
      "the evaluated command can write its workspace, cannot persist a write outside it, receives either an explicit kernel socket-creation denial or an IPv4 route denial while the outer control reaches the same frozen endpoint, and cannot read the outer credential sentinel",
      "the exact final candidate digest passes host-control-loss PID-namespace reaping"
    ],
    "credential_boundary": "the candidate build and probes contain no provider credential and make no model call"
  },
  "result_free_outer_boundary_candidate": {
    "launcher": "tools/launch_target_drift_agent_container.py exposes two fixed result-free component modes: the original offline boundary probe and one production-shaped excluded-provider execute path. Both accept an exact local image digest through the image SBOM, the hash-observed canonical Docker runtime, the frozen AppArmor source, one fixed fake auth.json, one fixed read-only input, and one initially empty root-control output directory",
    "docker_boundary": "the canonical argv uses a read-only root filesystem, the abrl-target-drift-codex AppArmor profile, no-new-privileges, a private PID namespace with abrl-agent-pid1 as PID 1, fixed CPU/memory/PID ceilings, all capabilities dropped except SETUID, SETGID, CHOWN, DAC_OVERRIDE, and FOWNER for the trusted root controller, and no Docker init shim",
    "root_controller": "the in-image root controller accepts no caller command; it validates the read-only single-file fake auth and agent input, copies only the agent input into disposable tmpfs, opens one read-only handoff descriptor after exact-byte validation, passes that descriptor to the irreversibly dropped numeric uid/gid 10002 worker, and closes its own descriptor after child creation",
    "model_shell_boundary": "the trusted worker must fstat and exactly consume the one-time fixed fake-auth descriptor, prove it is read-only, close it, and remove its environment marker before launching the fixed offline codex sandbox probe; the nested shell must receive EACCES or EPERM from the still-existing outer auth mount and root control output and must observe neither the broker descriptor environment marker nor an open descriptor targeting the auth mount. This component handoff is not the real Codex provider authentication path. Both the outer worker and nested shell independently read /proc/self/status and must report CapEff=0, while the nested shell has a writable tmpfs workspace, immutable original input, and denied network",
    "persistent_output": "before the worker starts, root creates a fixed 0400 sentinel in the 0700 root-control bind; the nested shell must receive EACCES or EPERM, not ENOENT, when reading that existing sentinel. On success the bind contains exactly the sentinel, two PID-1 ledgers, and root controller report, whose report binds the sentinel path/mode/uid/gid/bytes/SHA-256. On failure only, one additional bounded and fake-input-redacted worker diagnostic may be retained and can never satisfy the success evidence set",
    "evidence_status": "component candidate only: CI uses a literal fake sentinel, performs no provider request or model invocation, and neither publishes nor freezes a production image"
  },
  "result_free_excluded_execute_candidate": {
    "mode": "result_free_excluded_execute_v1",
    "request_contract": "one checked-in request fixes excluded_fixture, zero provider/model/network/cost budgets, primary_result_eligible=false, and a five-file production-shaped output surface; a checked-in invocation contract binds its bytes, the excluded adapter bytes, and the exact argv",
    "docker_boundary": "the same root PID-1 and uid/gid 10002 transition is used with Docker network none; no caller-controlled adapter command is accepted",
    "auth_boundary": "the controller accepts only the literal fixed fake auth bytes, opens one read-only descriptor, passes it to the dropped worker, and closes the root copy immediately after spawn; the excluded adapter consumes and closes the descriptor before creating artifacts and cannot read the outer auth or root-control paths",
    "adapter_boundary": "the excluded adapter imports no provider, network, or subprocess client; it emits one excluded_fixture attempt ledger, a one-event zero-usage trace, no Lean patch, no build, no public declaration, and a blocked component-only result",
    "validation": "the controller independently validates request/adapter hashes, response identity, zero token/tool/build/retry/cost usage, trace order, exact output file set and hashes, fake-auth handoff, and false provider/result flags; the host launcher revalidates the controller report and clean PID-1 child exit",
    "evidence_status": "strictly result-ineligible component evidence only; it is not the real three-condition smoke, does not alter execution_not_started, and cannot enter the 450-run ledger"
  },
  "result_free_production_action_fixture_candidate": {
    "mode": "result_free_production_action_fixture_v1",
    "request_contract": "one checked-in three-file input manifest fixes /agent/run, an offline prompt and Lean workspace, codex_cli with a hash-bound local fake executable, false provider/credential/network/result flags, model-call budget zero, and zero token/tool/build/cost budgets",
    "adapter_path": "the numeric uid/gid 10002 driver consumes and closes the fixed fake auth descriptor, materializes one temporary auth.json in tmpfs, launches the in-image codex_target_drift_adapter.py with the contract argv, and removes the temporary auth tree before reporting",
    "fake_provider": "the hash-bound local executable imports no network or subprocess client, accepts only the fixed Codex exec shape, writes a blocked result-ineligible fixture result, and emits deterministic zero-usage Codex JSONL; Docker network is none",
    "validation": "the root controller independently validates the input/driver/adapter/fake-provider hashes, response identity, one fake codex_cli attempt, one-event zero-usage trace, exact adapter/output file sets, no Lean diff or build, fake-auth closure/removal, and false provider/result flags",
    "copyback": "after validation, root copies the exact five adapter receipts and five output files to a separate host bind, writes a result-ineligible copyback receipt, and the host launcher re-hashes every copied byte against both controller evidence and receipt",
    "evidence_status": "production-shaped interface fixture only: it traverses the built-in Codex adapter but performs no provider/model/network call, is permanently result-ineligible, does not alter execution_not_started, and cannot enter smoke, primary, grading, or analysis ledgers"
  },
  "production_action_interface_candidate": {
    "contract": "agent-production-action-contract.json defines the final seal schema and exact Linux/Docker/image-SBOM/runtime/config/provider/probe/request bindings required before execution",
    "launcher_behavior": "production-execute accepts only a structurally final seal and rejects candidate, fake, fixture, result_free, unsealed, and UNSET values; after validation it still exits before credential inspection or Docker launch because production_execution_enabled=false in tracked source",
    "gate_status": "closed pending external Linux final-digest evidence; this is a strict contract/controller/launcher candidate, not an enabled provider action"
  },
  "production_gate": [
    "review and enable a real-provider execution action from the closed production-action interface while preserving the exercised sealed request/argv, root-only control, non-root tmpfs, PID-1, trace/output/copyback, and lifecycle boundaries",
    "mount one real provider credential through the frozen single-file boundary and rerun the full final-image credential-visibility probe without recording secret bytes or a secret-derived digest",
    "enforce and attest provider-only egress for the trusted client while keeping model-spawned shells network-disabled",
    "freeze the exact provider/model/settings/prices/budgets and publish the final image digest plus all final-image probes",
    "pass the permanently result-ineligible one-case-by-three-condition real-infrastructure smoke before any primary run"
  ],
  "production_nonclaims": [
    "A lifecycle candidate does not freeze the final provider-capable agent image.",
    "Network, filesystem, auth, provider-client, toolchain, and budget isolation require their own final-image probes and attestations.",
    "No lifecycle probe is a model run or formalization outcome."
  ]
}
