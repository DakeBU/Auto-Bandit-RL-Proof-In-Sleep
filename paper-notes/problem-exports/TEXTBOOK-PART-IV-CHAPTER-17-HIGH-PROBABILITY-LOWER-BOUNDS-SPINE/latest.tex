% Synchronized partial proof export for
% TEXTBOOK-PART-IV-CHAPTER-17-HIGH-PROBABILITY-LOWER-BOUNDS-SPINE
\section{Chapter 17 high-probability lower-bound dependency slice}

\paragraph{Status.}
The declarations named below compile. They are dependency leaves, not proofs
of Theorem~17.1, Corollaries~17.2--17.3, Theorem~17.4, Claim~17.6, or
Claim~17.7.

\paragraph{Compiled Lean declarations.}
\begin{itemize}
  \item the three exact stochastic/adversarial threshold definitions and
    \texttt{LowerBounds.tailAtLeast};
  \item \texttt{LowerBounds.exists\_tailMass\_ge\_of\_integral\_ge} and its
    CDF-tail specialization;
  \item \texttt{LowerBounds.measureReal\_diff\_ge\_delta};
  \item \texttt{LowerBounds.adversarialRegretLowerExpression}, its
    quarter-horizon lower bound, and the Eq.~(17.8) transfer theorem.
\end{itemize}

\paragraph{Proof map.}
The threshold definitions retain the source constants and logarithm
arguments: the outer factor $1/4$ in Theorem~17.1, the stochastic
$\log(1/(4\delta))$, and the adversarial $\log(1/(2\delta))$.

Claim~17.5 is the first-moment selection step. If an integrable function
$g(x)$ has expectation at least $\delta$ under a probability measure, an
integral-value witness gives an $x$ with $g(x)$ at least that expectation.
Taking $g(x)=1-F_x(u)$ yields the source CDF-tail conclusion. Separately,
$P(A)-P(B)\le P(A\setminus B)$ turns the bounds $P(A)\ge2\delta$ and
$P(B)\le\delta$ into $P(A\setminus B)\ge\delta$. Finally,
$T_i(n)\le n/2$ and at most $n/4$ clipped rounds make the right side of
Eq.~(17.8) at least $\Delta n/4$.

\paragraph{Boundary.}
The stochastic terminal still lacks the adaptive-history likelihood-ratio/KL
decomposition for one common possibly randomized policy. The adversarial
terminal still lacks the shared-noise clipped-normal construction and the
proofs of Claims~17.6--17.7. No hard-instance existence theorem, deterministic
reward-matrix existence theorem, or other Chapter~17 source terminal is
claimed.
