{
  "master": "codex exec --cd {root} < {prompt}",
  "worker": "codex exec --cd {root} < {prompt}",
  "upper": "codex exec --cd {root} < {prompt}",
  "middle": "codex exec --cd {root} < {prompt}",
  "lower": "codex exec --cd {root} < {prompt}",
  "reviewer": "codex exec --cd {root} < {prompt}"
}
